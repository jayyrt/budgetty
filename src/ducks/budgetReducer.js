const initialState = {
    purchases: [],
    budgetLimit: null,
    loading: false,
}

function reducer(state, action){
    switch(action.type){
        default: {
            return state;
        }
    }
}

export default reducer;